# I-032 — Implémentation (Opus, 2026-09-07)

Résumé du prompt (règles habituelles : pas de sous-agents, budget d'outils, lecture de CONVENTIONS/STATUS/ticket/research avant d'écrire, aucune validation redondante, rien de committé, section Livraison en français, STATUS KPI) :

T-032 — `src/app/view_plan.c` : liste virtualisée des entrées du disque courant (n°, badge de mode cliquable SP/LP2/LP4/mono cyclant via `plan_set_mode`, titre MD **tel qu'il sera écrit** via `plan_toc_preview` avec pictogramme quand une étape de raccourcissement a joué, artiste/album d'origine, durée, coût en clusters), drag & drop interne (fantôme, ligne d'insertion, autoscroll, une seule commande `plan_move` au drop), Alt+↑/↓, Suppr, Ctrl+Z/Y, F2 renommage inline (`plan_set_title`), sélection multiple (Shift/Ctrl, Ctrl+A) et changement de mode groupé, groupes repliables à en-tête éditable, menu contextuel, indicateur piste manquante (`track_id` non vivant et chemin absent).

Jauge de capacité **au pixel** de research/02 §9.2–9.10 (56 px, segments par piste colorés par mode avec alternance de luminosité, hachures du padding de cluster à partir de `PlanCapacity.clusters` vs `duration_ms`, échelle tri-modale, ligne de lecture au survol, zone de dépassement rouge hachurée dès `first_overflow`, tooltip par segment, variante compacte 12 px dans la barre de statut, animation 120 ms à chaque changement de révision) ; barre TOC §9.7 (`PlanTocBudget` : part disque vs pistes, rouge si `overflow`, bouton « raccourcir automatiquement » qui applique `plan_toc_preview` avec `max_chars` = quota par piste comme override de titre en **une** commande groupée).

Entrée depuis la bibliothèque : Entrée / double-clic / bouton / DnD bibliothèque → plan avec le mode par défaut du plan, « remplir l'espace restant » (`plan_capacity_would_fit` dans l'ordre de la sélection), « nouveau disque » quand ça déborde (le plan passe multi-disques : onglets 1..n, `plan_capacity_split` derrière un bouton « répartir automatiquement » avec les deux politiques). En-tête : titre de disque éditable, 60/74/80, mode par défaut, Ouvrir / Enregistrer / Enregistrer sous via `os_dialog_*`, pastille d'autosave, `Str_*` FR/EN pour tout nouveau texte.

Tests `tests/test_view_plan.c` : interactions → commandes (DnD → Move avec les bons index, badge → SetMode, Suppr multi → une commande), géométrie de la jauge (somme des largeurs = largeur, ordre, hachures et zone rouge aux bons offsets) en isolant la géométrie dans une fonction pure `plan_gauge_layout` testable sans GL. Cibles mesurées : frame de la vue Plan avec 254 entrées < 1,5 ms de CPU (bench), 60 fps en DnD, 0 % CPU au repos (protocole STATUS), exe < 330 KB, tous les `build.bat` verts, captures via `tools/capture_demo.ps1`.
