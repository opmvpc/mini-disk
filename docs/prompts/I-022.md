# I-022 — Implémentation (Opus, 2026-09-07)

Résumé du prompt (règles habituelles, worktree `mini-disk-t022`, interdiction d'écrire sur le vrai disque hors un renommage aller-retour après
sauvegarde TOC, et seulement si WinUSB est là) : T-022 — `netmd_edit` (titres disque/piste avec `oldLen` exact et wchar 0x00/0x01 vs
0x02/0x03, move, erase, groupes par réécriture du titre disque via `plan_toc_compile_disc_title`, délais §6.2, ordre §6.5) ; simulation D4 →
`DiscDiff` avant toute écriture, refus sur dépassement TOC ou disque protégé ; sauvegarde TOC texte atomique + rechargement ; commandes du
thread device avec relecture après écriture et drapeau « TOC en RAM » ; panneau Disque (F2, DnD, Suppr, groupes, bannière, fermeture bloquée) ;
tests par rejeu de transcriptions d'édition synthétiques, règle `oldLen`, diff sur 10 cas limites, round-trip de sauvegarde. Exe < 500 KB.
