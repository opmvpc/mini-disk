# I-075 — Implémentation (Opus, 2026-09-08)

Résumé du prompt (règles habituelles : worktree `mini-disk-t075` coupé de `main` après les merges de T-070,
T-071 et T-072 ; un seul agent, sans sous-agents ; commandes au premier plan ; six cibles `build.bat`
vertes ; jamais d'écriture sur l'appareil ; zones disjointes de T-073 qui tourne en parallèle) : voir le ticket
T-075 pour les livrables et l'annexe Codex pour la seconde revue. Onze décisions déjà prises listées dans le
prompt (jetons `control_h`/`row_control`, `app_button_row` sur `ui_flow`, transport en icônes, `UI_Clip`,
lignes multi-lignes, bulle qui bascule, minima 360/400 dp, jauge, compteurs de la barre de statut en debug,
finitions Préférences et alias `?`), quatre refus explicites, captures après aux deux tailles regardées par
l'agent avant de rendre, exe ± 10 KB de 655 360 o.

Genèse : revue visuelle du lead sur trois captures (`T-075-avant-*.png`), puis contre-revue demandée par
l'utilisateur à un modèle externe via `codex exec -m gpt-6-astra -i <captures>` (annexe T-075).
