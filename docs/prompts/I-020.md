# I-020 — Implémentation (Opus, 2026-09-07)

Résumé du prompt (règles habituelles, worktree `mini-disk-t020`, jamais d'action sur les pilotes) : T-020 — contrat USB dans `platform.h`
(énumération avec états Ready/NoDriver/InUse, open/close/control/bulk/reset), `win32_usb.c` avec winusb/setupapi/cfgmgr32 chargées dynamiquement,
device sans pilote (code 28) signalé avec son nom bus, GUID lu dans le registre, politiques de pipe de research/01 §2.3 ; hotplug
`RegisterDeviceNotification` toutes classes → `OsEvent_DeviceChange` avec débounce 200 ms ; `UsbTransport` (ADR-008) + transport de rejeu `.trace` ;
thread device avec files commandes/résultats façon `lib_events`, ping = poll/send/read ; écran Disque en trois états avec les étapes Zadig ;
tests de rejeu et de la table des PIDs ; revérifier `Get-PnpDevice` avant validation finale. Cibles : exe < 340 KB, tous les `build.bat` verts.
