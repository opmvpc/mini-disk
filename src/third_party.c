// third_party.c - unity build of third party libraries (minimp3, dr_*, stb_*).
// Empty until the codecs land; kept as a separate .obj so their macros never leak
// into our own translation unit (ADR-002).

void third_party_unity_placeholder(void);
void third_party_unity_placeholder(void) {}
